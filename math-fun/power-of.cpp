#include <iostream>
#include <cmath>
using namespace std;

int main()
{

    double num;
    cout << "Enter a number: ";
    cin >> num;
    cout << pow(num, 2) << endl;
    cout << pow(num, 3) << endl;
    cout << pow(num, 4) << endl;

    return 0;
}