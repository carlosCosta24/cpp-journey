#include <iostream>
using namespace std;

int main()
{
    cout << "the size of bool is :" << sizeof(bool) << endl;
    cout << "the size of char is :" << sizeof(char) << endl;
    cout << "the size of sort int is :" << sizeof(short int) << endl;
    cout << "the size of int is :" << sizeof(int) << endl;

    return 0;
}